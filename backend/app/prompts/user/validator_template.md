# Claim narrative

{claim_narrative}

# Retrieved policy chunks

The following chunks were retrieved from the carrier's commercial property policy by cosine-similarity search against the narrative above. They are the **only** authority you may cite.

{retrieved_chunks}

# Task

Decide whether the policy covers the loss described in the narrative, citing **only** the retrieved chunks. Return the JSON object specified in your system instructions — no preamble, no Markdown fencing, no trailing prose.
