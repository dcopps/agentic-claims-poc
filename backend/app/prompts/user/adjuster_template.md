# Claim summary

{claim_summary}

# Validator verdict

{validator_verdict}

# Market range constraint

- Claim type: `{claim_type}`
- Severity: `{severity}`
- Settlement value MUST be in the range **[{range_floor}, {range_ceiling}]** (USD, inclusive on both ends).

# Task

Recommend a settlement value within the market range above. Justify it briefly in terms of damage scope and range positioning — do **not** cite policy. Return the JSON object specified in your system instructions, with no preamble, no Markdown fencing, no trailing prose.
