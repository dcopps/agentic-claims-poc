"""
Externalised prompt directory.

Phase 2 introduces a `PromptLoader` that reads from `system/` and
`user/`. Phase 1 only creates the directory structure so the loader has
a stable contract to land against.
"""
